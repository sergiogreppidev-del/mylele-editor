# Copiá este archivo como  .env  y completá los valores.
# Estas dos variables van también en Vercel (Settings -> Environment Variables).
#
# IMPORTANTE: acá va SOLO la clave publicable (anon). Es de solo lectura sin login
# y ya está expuesta en la app de alumnos, así que no es un secreto.
# La clave service_role NUNCA va en este archivo, ni en el repo, ni en Vercel.

VITE_SUPABASE_URL=https://duvflmqbagnlhznuqjhr.supabase.co
VITE_SUPABASE_ANON_KEY=pega-aca-la-anon-key
